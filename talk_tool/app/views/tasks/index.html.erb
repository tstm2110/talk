<h1>TASKS INDEX</h1>
<table>
  <thead>
    <tr>
      <th>name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.title %></td>
        
        
        <td><%= link_to 'edit', edit_task_path(task) %></td>
        <td><%= link_to 'delete', task_path(task), method: :delete %></td>
      
      </tr>
    <% end %>
  </tbody>
  <thead>
      <td><%= link_to 'add', new_task_path, class:"left" %></td>
  </thead>
</table>
 
